To build the package:
(uses mastershaper as an example...works the same for other packages)

1) cd mastershaper_deb
1) fakeroot dpkg-deb --build debian
2) mv debian.deb mastershaper_0.44_all.deb
3) lintian mastershaper_0.44_all.deb

DONE!
